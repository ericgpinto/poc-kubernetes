apiVersion: v1
kind: Service
metadata:
  name: python-service
spec:
  ## Match the label defined in deployment.yaml
  selector:
    app: python-server
  type: ClusterIP
  ports:
    - name: python-service
      port: 8080
      protocol: TCP
